# Agent Notes

- When backend management capabilities change, update the local `/Users/mageia/.agents/skills/newapi` skill in the same change.
- The `model-pricing-rules` capability is synchronized: it manages exact user or user-group model billing ratios without changing access or routing.
